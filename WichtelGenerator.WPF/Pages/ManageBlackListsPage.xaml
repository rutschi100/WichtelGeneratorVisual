<Page x:Class="WichtelGenerator.WPF.Pages.ManageBlackListsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="ManageBlackListsPage"
      Background="White">

    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0"
                   Content="Wähle zu konfigurierender Wichtel" />
            <ComboBox Grid.Row="0" Grid.Column="1"
                      ItemsSource="{Binding ActiveSantas.Name}"
                      SelectedItem="{Binding SelcectedSanta}" />

            <Label Grid.Row="1" Grid.Column="0" Background="AliceBlue">Verfügbare Wichtel</Label>
            <Label Grid.Row="1" Grid.Column="1" Background="AliceBlue">Wichtel auf der Blacklist</Label>

            <ListView Grid.Row="2" Grid.Column="0" ItemsSource="{Binding SelectedSantaModel.WhiteList}" />
            <ListView Grid.Row="2" Grid.Column="1" ItemsSource="{Binding SelectedSantaModel.BlackList}" />

        </Grid>
    </StackPanel>
</Page>