<Page x:Class="WichtelGenerator.WPF.Pages.RufflePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="RufflePage"
      Background="White">

    <StackPanel>
        <Label Background="AliceBlue">Einstellung</Label>
        <RadioButton GroupName="RuffleVisible" IsChecked="{Binding ResultPerMail}">Per Mail</RadioButton>
        <RadioButton GroupName="RuffleVisible" IsChecked="{Binding ResultPerGui}">Hier anzeigen</RadioButton>
        <Button Margin="2" Background="Green"
                Command="{Binding OnRuffle}">Verlosung auslösen</Button>

        <Label Background="AliceBlue" Visibility="{Binding ResultVisibility}">Verlosungsresultat</Label>
        <DataGrid Visibility="{Binding ResultVisibility}"
                  ItemsSource="{Binding ResultList}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Wichtel" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Beschenkt" Binding="{Binding Choise.Name}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Page>